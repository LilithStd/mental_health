'use client'
interface ModalWindowUserProblemDescriptionProps {
    closeCallback: () => void,
}
export default function ModalWindowUserProblemDescription({ closeCallback }: ModalWindowUserProblemDescriptionProps) {

    return (
        <div className={`flex flex-col gap-4 justify-center relative items-center`}>
            <h2>modalWindowUserProblemDescription</h2>
        </div>
    )
}
